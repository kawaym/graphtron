use graphtron::Graph;
use graphtron::Vertex;

#[cfg(test)]
#[test]
fn it_works() {
    let mut _graph = Graph::new(1, None).maximum_degree();
    let _graph_info = _graph;

    let _vertex = Vertex::new(1);
}
